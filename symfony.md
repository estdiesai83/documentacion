# Symfony

https://thepower.education/blog/tech/symfony-el-framework-php

Symfony es un **framework de código abierto** basado en PHP y diseñado para construir aplicaciones web modernas, robustas y totalmente escalables. Se apoya en el patrón de arquitectura [**Modelo–Vista–Controlador (MVC)**](https://thepower.education/blog/tech/modelo-vista-controlador-mvc), lo que permite separar la lógica de negocio de la presentación y mantener un código limpio, organizado y fácil de mantener.

Mientras otros frameworks PHP están orientados a proyectos más simples, Symfony destaca por su capacidad de adaptarse a **proyectos de gran envergadura**, sistemas con muchos módulos o plataformas que requieren un rendimiento sólido y estabilidad a largo plazo.

###  ¿Qué hace tan especial a Symfony?

- Está construido **siguiendo principios de programación orientada a objetos (OOP)**.
- Su arquitectura es altamente modular: puedes usar el framework completo o solo algunos de sus **+50 componentes reutilizables**.
- Facilita la implementación de **buenas prácticas**, como [SOLID](https://thepower.education/blog/tech/principios-solid) o DRY.
- Es uno de los [frameworks](https://thepower.education/blog/tech/framework-acelera-el-desarrollo) más **flexibles**, personalizables y preparados para entornos empresariales.

Symfony también destaca por su **madurez**, su gran estabilidad y su compatibilidad con herramientas de desarrollo modernas, gestoras de bases de datos, sistemas de cacheo y librerías front-end.

Por esto, es una de las opciones favoritas para construir aplicaciones de alto nivel como marketplaces, sistemas de reservas, plataformas SaaS o herramientas internas complejas.

## Elementos de Symfony framework: 

Symfony incluye componentes PHP, un marco de aplicación, una comunidad y una filosofía propia. Todos estos elementos funcionan con Symfony para lograr fácilmente el objetivo general de cada proyecto. A continuación te contamos algunos de los elementos del Symfony PHP framework:

- **Framework** PHP Symfony: Este es un framework PHP esencial que envuelve los componentes de Symfony para hacer sitios ricos en características y aplicaciones en línea.
- **Componentes** PHP de Symfony: Los segmentos son partes reutilizables que un diseñador web puede utilizar para montar hermosas aplicaciones PHP. También incluyen phpBB, Drupal y eZ Publish.
- **Comunidad** Symfony PHP: Muchos diseñadores de PHP forman parte de esta red y contribuyen al desarrollo continuo de Symfony.
- **Filosofía** Symfony PHP: La filosofía de Symfony tiene como objetivo avanzar en los procedimientos prescritos, mejorar las habilidades y aumentar la estandarización y la capacidad de las aplicaciones electrónicas.

## **5 Características clave de Symfony**

Además de la arquitectura [MVC](https://thepower.education/blog/tech/modelo-vista-controlador-mvc) y la orientación a las buenas prácticas, Symfony también ofrece las siguientes características: 

### 1. **Componentes reutilizables**

Symfony cuenta con más de **50 componentes independientes**, completamente reutilizables. Cada uno de estos componentes puede ofrecer funcionalidades específicas y pueden usarse de manera aislada en cualquier proyecto PHP.

Los componentes reutilizables son bastante flexibles. Por un lado, pueden trabajarse de forma específica sin afectar al resto de los componentes. Por otro lado, pueden combinarse de diferentes maneras para construir aplicaciones webs.

El resultado, una web robusta, sólida, eficiente y escalable con una gran variedad de funcionalidades.

**Como dato adicional:**  este modelo de software es perfecto para poner en práctica los [Principios SOLID](https://thepower.education/blog/tech/principios-solid) para el desarrollo de software. 

### **2. Enrutamiento flexible**

Una de las funcionalidades más útiles de Symfony y de sus componentes reutilizables es el enrutamiento flexible.

Esta función es la que permite definir y **gestionar las rutas de las URLs de manera fácil pero eficiente**. Y es la responsable de responder correctamente a las peticiones de los usuarios y facilitar su experiencia dentro de la aplicación y de la web.

Además, este framework cuenta con una buena variedad de funciones para generar URLs automáticamente según las rutas definidas. En otras palabras, los enlaces son más funcionales y no tienes que escribirlos de forma manual.

Sin mencionar que una estructura limpia es mucho más eficiente, tanto para el usuario como para el funcionamiento de la aplicación.

### 3. **Gestión de sesiones**

Desde el almacenamiento hasta la configuración, manipulación y extensión de sesiones, Symfony te permite manejarlo todo dentro de tu aplicación web.

Este framework cuenta con diferentes herramientas para la gestión de sesiones, tales como **bases de datos y sistemas de almacenamiento integrables**. También te permite gestionar sesiones directamente por controlador, lo que te ayudará en caso de errores y problemas de sesión.

Además, cuenta con altos estándares para la protección de sesiones contra falsificación de solicitudes y robo de sesiones, entre otras amenazas.

### 4. **Versatilidad y compatibilidad**

Symfony puede instalarse en cualquier sistema operativo, ya sea **Windows, Mac, e incluso Linux**. Sin mencionar que puede configurarse rápidamente.

Además, es compatible con una amplia gama de herramientas, incluso aunque está pensado para grandes proyectos. Por tanto, puedes combinarlo con otros frameworks y aplicaciones para casi cualquier tipo de proyectos de desarrollo web. 

De hecho, también puede adaptarse a bases de datos y complementar herramientas enfocadas a la ciencia de datos. 

### 5. **Capa de internacionalización y localización**

Symfony facilita la internacionalización de aplicaciones web. En otras palabras, puede ayudarte a **traducir tu web y adaptarla a diferentes idiomas, dialectos y regiones**.

Symfony ofrece herramientas de traducción instantánea y selección del idioma, pero eso no es todo. También cuenta con **opciones de localización, formatos de números y monedas**. A su vez, cuenta con un componente de traducción para gestionar las traducciones y la aplicación en sus diferentes idiomas. 



## **6 Ventajas de usar Symfony**

### **Facilidad para implementar buenas prácticas**

El modelo de Symfony fomenta y facilita las buenas prácticas para el desarrollo de software. Y lo mejor, gracias a su estructura y a sus diferentes funciones, **puedes incorporar las buenas prácticas sin siquiera notarlo**.

Además, este framework es un ejemplo de libro de cómo trabajar la programación orientada a objetos y sus diferentes metodologías. 

Estas prácticas se traducirán en **un código mucho más limpio, legible, fácil de mantener y fácil de compartir**. Lo que mejorará la coordinación del equipo de desarrollo y eliminará problemas de código heredado al trabajar con otros equipos.

### 2. **Capacidad de integración**

**Porque Symfony** no solo promueve las buenas prácticas, sino que puede trabajar con otros lenguajes como CSS y JavaScript e integrarse perfectamente a otras tecnologías. Por ejemplo, puede combinarse con bibliotecas y frameworks front-end, tales como **Angular y React** para construir interfaces de usuario.

Además, también puede trabajar con **SQL** para interactuar con bases de datos. Por ejemplo, es perfecta para trabajar en **archivos XML**, como sitemaps, o **archivos YAML**. Y funciona de maravilla al ejecutar migraciones de bases de datos. 

También puede combinarse con DQL y **Doctrine**, su herramienta para **ORM (Mapeo Objeto-Relacional por sus siglas en inglés)**, para manejar bases de datos avanzadas y consultas complejas. 

### 3. **Alto rendimiento**

Y esta es la razón por la que muchos desarrolladores prefieren Symfony para proyectos más ambiciosos.

Symfony no solo tiene una gigantesca variedad de herramientas. También puede mejorar el rendimiento de tus aplicaciones web a través de las características ya mencionadas.

Por ejemplo:

- Entre sus herramientas de manejo de sesiones, puedes encontrar un excelente sistema de caché, perfecto para reducir la sobrecarga por consultas frecuentes en el servidor.

- A través de Doctrine, se puede reducir la carga diferida y minimizar la sobrecarga de datos. También te permite optimizar consultas para reducir los tiempos de respuesta del sistema. 

Entre muchos otros casos.

Además, Symfony también cuenta con un profiler, un componente fundamental para analizar y optimizar el rendimiento de tu aplicación. Con él, puedes detectar y eliminar cuellos de botella, consultas duplicadas y errores de ejecución.

### 4. **Vasta documentación**

En el mundo de la programación, las bibliotecas y bases de conocimientos son más que invaluables. Y Symfony tiene mucha ventaja en este campo.

Este framework cuenta con una enorme biblioteca, tanto para el aprendizaje y la compresión de la interfaz como su implementación en el desarrollo de proyectos. 

También cuenta con bastantes recursos para profundizar tanto en Symfony como en Doctrine y sus otras herramientas integradas. Y como Symfony todavía goza de mucha popularidad, la información se actualiza constantemente, por lo que siempre encontrarás datos y recursos actualizados.

### 5.**Eficiencia y escalabilidad**

Todos los puntos anteriores se traducen en más eficiencia y más recursos para tus aplicaciones web.

El Modelo Vista-Controlador no solo facilita la redacción de código limpio, sino el desarrollo a través de modelos escalables, tales como la distribución de carga del servidor (load balancing) y la replicación de servidores, entre otras características. 

Además, puedes ampliar y optimizar tus aplicaciones como desees para hacerlas más eficientes a corto plazo, y más escalables a largo plazo.

### 6. **Comunidad activa**

Cuenta con una comunidad activa de miles de desarrolladores y especialistas, quienes pueden darte más recursos, responder consultas y echarte una mano cuando sea necesario.

Symfony es, junto a Laravel, el framework PHP favorito para el desarrollo web.



## **Consideraciones de este framework**

Symfony es un framework poderoso, estable y altamente profesional… pero como toda tecnología avanzada, también tiene ciertos aspectos que debes conocer antes de adoptarlo en un proyecto. Entender estas consideraciones te permitirá evaluar cuándo Symfony es la mejor opción y cuándo conviene elegir alternativas más ligeras.

### 1. Un framework pesado para proyectos pequeños

La arquitectura de Symfony está diseñada para **escalar**, no para soluciones mínimas o aplicaciones pequeñas con pocos módulos.
Su estructura, sus componentes y su potencia pueden resultar **excesivos** cuando el proyecto:

- Tiene pocas rutas
- Requiere un backend ligero
- No necesita funcionalidades avanzadas
- Se ejecuta en servidores con recursos limitados

Symfony consume más recursos que frameworks más simples, por lo que puede no ser la opción ideal para [VPs](https://thepower.education/blog/tech/vps-que-es-para-que-sirve-y-por-que-lo-necesitas) rápidos, proyectos de bajo coste o aplicaciones que solo requieren lógica muy básica.

**Pero…**
Cuando entras en proyectos medianos o grandes, todo ese “peso extra” se convierte en una **ventaja**, porque dispones de herramientas avanzadas, una arquitectura robusta y una base sólida para crecer sin limitaciones.

### 2. Curva de aprendizaje considerable

Aunque Symfony es muy amigable si ya sabes PHP, dominarlo en profundidad requiere tiempo. Su curva de aprendizaje es mayor que la de otros frameworks porque:

- Está muy orientado a **OOP**, patrones de diseño y buenas prácticas.

- Utiliza estructura y conceptos avanzados (bundles, servicios, contenedores, eventos…).

  * En el framework Symfony, un **bundle** es un conjunto estructurado de archivos (controladores, vistas, entidades) que comparten funcionalidad, comúnmente organizados en un `AppBundle`.

  * El **contenedor** de servicios de Symfony es el núcleo de su arquitectura, encargado de gestionar, instanciar y organizar objetos (servicios) y sus dependencias. Facilita la reutilización de código y el desarrollo modular. Se puede interactuar con contenedores Docker para Symfony usando `docker exec -it`, o alojar aplicaciones con [Back4App Containers](https://www.back4app.com/docs-containers-es/guia-tecnica-para-desplegar-symfony-en-back4app-containers). 

    [Aspectos clave del contenedor de servicios en Symfony:]()

    - **Gestión de Dependencias:** El contenedor crea automáticamente los objetos necesarios y sus dependencias (inyección de dependencias).
    - **Servicios Inherentes:** Incluye servicios esenciales como `Session`, `Doctrine` (bases de datos) o `FileSystem`.
    - **Organización:** Permite una estructura más limpia y modular, lo que facilita el mantenimiento de aplicaciones complejas. 

    [Contenedores Docker con Symfony:]()

    - **Uso:** Se utilizan comúnmente para el desarrollo y despliegue, permitiendo aislar la aplicación.

    - **Acceso:** Con `docker exec -it <nombre_contenedor> bash`, se obtiene acceso interactivo a la terminal del contenedor.

    - **Configuración:** Generalmente definido en un archivo `docker-compose.yaml`.

       

- Sus componentes ofrecen múltiples configuraciones posibles.

Esto puede ser un reto, pero también un enorme diferencial profesional:
**Symfony es uno de los frameworks que más valoran las empresas cuando buscan perfiles PHP experimentados.**

En otras palabras:
Cuesta aprenderlo, pero te abre puertas a proyectos más serios y mejor remunerados.

## **¿Qué debo saber para aprender Symfony?**

Symfony es un framework avanzado y orientado a buenas prácticas, por lo que dominarlo requiere cierta base técnica previa. No necesitas ser un experto, pero sí manejar conceptos clave que te permitirán aprovechar su verdadero potencial.

Aquí tienes los conocimientos esenciales para empezar con Symfony con buen pie:

### 1. **PHP (el requisito número uno)**

Symfony está construido completamente en **PHP**, así que este es el punto de partida.
No solo necesitas saber PHP básico:

- Estructuras
- Funciones
- Manejo de errores
- Namespaces
- Programación modular

También es recomendable conocer las **características modernas de PHP 7+ / PHP 8+**, ya que Symfony aprovecha muchas de ellas (tipado, clases avanzadas, atributos, etc.).

> Puedes aprender PHP y Symfony al mismo tiempo, pero avanzarás mucho más rápido si primero dominas bien el lenguaje.

### 2. **Programación Orientada a Objetos (OOP)**

Symfony está profundamente alineado con la OOP.
Para comprenderlo bien, necesitas manejar:

- Clases y objetos
- Herencia
- Interfaces
- Métodos mágicos
- Encapsulación
- Composición
- Principios **SOLID**

Symfony está construido apoyándose en estos principios, así que cuanto mejor entiendas OOP, más fácil será dominar el framework.

### 3. **SQL y bases de datos**

Gran parte del desarrollo con Symfony se apoya en bases de datos y en el uso de **Doctrine**, su ORM integrado.

Necesitas entender:

- Consultas SQL básicas
- Relaciones (1-1, 1-N, N-N)
- Normalización
- Transacciones
- Migraciones

Esto te permitirá trabajar correctamente con entidades, repositorios y consultas dentro del framework.

### 4. **HTML, CSS y JavaScript**

Symfony se integra con tecnologías front-end sin problemas.

No necesitas ser experto en frontend, pero sí tener claridad sobre:

- Estructuras HTML
- Estilos CSS
- Lógica básica de JavaScript
- Integración con frameworks como React o Angular (cuando sea necesario)

Esta base te permitirá construir vistas, integrar componentes o adaptar plantillas para diferentes proyectos web.





## Webs recomendadas por csi

Curso gratuito: https://symfonycasts.com/es/screencast/symfony

Libro para desarrolladores: https://symfony.com/doc/6.4/the-fast-track/en/index.html



# Curso de symfonycast:

Synfony es en realidad una colección de más de 200 pequeñas librerías PHP. Así que son un montón de herramientas... pero podemos elegir lo que necesitamos.

Porque puedes estar construyendo una API pura... o una aplicación web completa.

API Platform es un sistema para crear APIS construido sobre Symfony.

## Instalación en Ubuntu

https://www.linuxparty.es/94-transportes/11787-como-instalar-el-framework-php-symfony-en-ubuntu-22-04.html

https://symfony.com/download

```bash
sudo wget https://get.symfony.com/cli/installer -O - | bash
sudo apt install symfony-cli
```

Definir la ruta del sistema Symfony en su sistema. Puedes hacerlo con el siguiente comando:

```bash
export PATH="$HOME/.symfony5/bin:$PATH"
source ~/.bashrc
```



## Instalar Symfony Packages

Puedes usar las funcionalidades de Symfony en cualquier aplicación PHP, no solo en proyectos Symfony. Symfony publica más de 250 paquetes independientes, por lo que puedes añadir solo lo que necesites a tus aplicaciones. Algunos de estos paquetes, llamados componentes, son autónomos y no requieren otros paquetes para funcionar.

Instala Composer y ejecuta este comando para añadir un paquete Symfony a tu aplicación:

```bash
composer require symfony/amphp-http-client-meta
```

Una vez que lo hayas descargado e instalado, abre un terminal y entra en cualquier directorio. Comprueba que el binario `symfony` está listo para funcionar ejecutándolo:

```bash
symfony --help
```

Antes de iniciar un proyecto, ejecuta tambien el siguiente comando para comprobar requisitos.  Esto asegura que tenemos todo lo necesario en nuestro sistema para ejecutar Symfony, como PHP en la versión correcta y algunas extensiones PHP.

```bash
symfony check:req
```

![image-20260325220518779](/home/soa/.config/Typora/typora-user-images/image-20260325220518779.png)

# Crear nuevo proyecto

```bash
symfony new --webapp my_project
```

Si se quiere construir un microservicio, aplicacion de consola o API:

```bash
symfony new my_project
```

Ejemplo:

```bash
symfony new starshop
```

Con este ejemplo crea solo las cosas bases y luego iremos añadiendo más cosas poco a poco, según vayamos necesitando, si se necesita un proyecto con muchas cosas preinstaladas se necesitaría el mismo comando pero con --webapp.

Con la opción básica crea la siguiente estructura:

![image-20260325220801871](/home/soa/.config/Typora/typora-user-images/image-20260325220801871.png)

Con -- webapp crea la siguiente estructura:

![image-20260325220726906](/home/soa/.config/Typora/typora-user-images/image-20260325220726906.png)

## Iniciar el servidor Web Symfony

Para verlo funcionando en un navegador, necesitamos iniciar un servidor web. Se puede utilizar Apache, Nginx, Caddy,.., pero para el desarrollo local se recomienda el binario :

```bash
symfony serve
o
symfony serve -d (se ejecuta en segundo plano)
```

La primera vez puede que te pida que ejecutes otro comando para configurar un certificado SSL, lo cual está bien porque así el servidor admite https.

![image-20260325221307197](/home/soa/.config/Typora/typora-user-images/image-20260325221307197.png)

Así tenemos un nuevo servidor web para nuestro proyecto ejecutándose en https:://127.0.0.1:8000



## Conociendo nuestro pequeño proyecto

Cuando se ejecuta el comando **symfony new** descargó un pequeño proyecto e inicializó un repositorio Git con una confirmación inicial.

Para ver los archivos confirmados:

```bash
git ls-files
```

![image-20260325222447071](/home/soa/.config/Typora/typora-user-images/image-20260325222447071.png)

### ¿Dónde está Symfony?

Uno de los archivos importante es : **composer.json**

```json
"require": {
        "php": ">=8.4",
        "ext-ctype": "*",
        "ext-iconv": "*",
        "symfony/console": "8.0.*",
        "symfony/dotenv": "8.0.*",
        "symfony/flex": "^2",
        "symfony/framework-bundle": "8.0.*",
        "symfony/runtime": "8.0.*",
        "symfony/yaml": "8.0.*"
    },
```

**Composer** es el gestor de paquetes de PHP, que lee los nombres de los paquetes bajo la clave **require** y los descarga. Cuando se ejecuta *symfony new*, descarga estos archivos y ejecuta *composer install*, estos paquetes se descargan en el directorio **vendor/**

#### Ejecuta composer

El directorio **vendor/** no está registrado en git. Se ignora gracias al archivo .**gitignore**.

![image-20260325223217248](/home/soa/.config/Typora/typora-user-images/image-20260325223217248.png)

Esto significa que si otro compañero clona nuestro proyecto, no tendrá este directorio, siempre podemos repoblarlo ejecutando **composer install**

### Los dos directorios que te importan

**config:** contiene la configuración.

**src:** contiene nuestro código php.



Los otros 4 directorios:

**bin:** contiene un único archivo ejecutable *console*, nunca modificaremos ese archivo

**public:** es la raiz del documento, cualquier cosa que pongas aquí como una imagen será accesible públicamente. También contiene **index.php**, se conoce como tu "controlador frontal": es el archivo PHP principal que tu servidor web ejecuta al inicio de cada petición, nunca editarás ni pensará en este archivo.

**var:** también se ignora desde git: es donde se almacena los archivos de registro y los de cache que necesita internamente.



## Creación del controlador

Todo empieza con la creación de la primera página, la idea detrás de cada página es siempre la misma. Pasos:

1. ruta: dale una URL chula.

2. escribir una función PHP que genere la página, eso se conoce como el controlador. Y esa página puede ser HTML, JSON, ...

   **En Symfony, el controlador es siempre un método dentro de una clase PHP.**

   Nota: La principal diferencia es que un **método** está asociado a un objeto o clase (programación orientada a objetos) y opera sobre sus datos, mientras que una **función** es un bloque de código independiente y reutilizable que puede llamarse desde cualquier lugar. Ambos realizan tareas, pero los métodos se llaman usando la sintaxis de punto (ej: `objeto.metodo()`).

3. Para crear un controlador: dentro del directorio **src/Controller/** crearemos un archivo *MainController.php* (se puede poner el nombre que quieras)

   ```php
   namespace App\Controller;
   
   class MainController
   {
   }
   ```



#### Espacios de nombres y directorios

Reglas Generales sobre las clases PHP:

1. Cada clase debe tener un espacio de nombres que coincida con tu estructura de directorios. Siempre será App\ y luego el directorio en el que estés.
2. El nombre de la clase debe de coincidir con el nombre de tu archivo .php. Si te equivocas en cualquiera de estas dos cosas, recibirás un error de PHP diciendo que no puede encontrar tu clase.



#### Crear el método controlador y la ruta

Añade una nueva función pública y llámala **homepage** (el nombre no importa).

```php
class MainController
{
    public function homepage()
    {

    }
}
```

Recuerda, una página es la combinación de un controlador y una ruta, que define la URL de la página.

La ruta la pondremos justo encima del método controlador, utilizando una función de PHP llamada **atributo**.

Escribe #[] y luego empieza a escribir Route con mayúscula R. ¡Fíjate en el autocompletado!

Cualquiera de las opciones funcionará, pero utiliza la de **Attribute** -que es más nueva- y luego pulsa tabulador, el editor debería añadir una declaración **use** al principio de la clase. Siempre que utilices el atributo PHP, debes tener una declaración **use** correspondiente para él en el mismo archivo.

Estos atributos funcionan casi como las funciones PHP: puedes pasar un montón de argumentos. El primero es la ruta, establécela en /.

```php
use Symfony\Component\Routing\Attribute\Route;

class MainController
{
    #[Route('/')]
    public function homepage()
    {

    }
}
```

Con esto, cuando se vaya a la página de inicio -**/**- Symfony llamará a este método controlador para construir la página.

#### Controladores y respuestas

¿Que debería devolver el método? Sólo el HTML o el JSON si estamos construyendo una API?

Por ejemplo, primero, un usuario solicita una página:

```
"Oye quiero ver /products ... o quiero ver /user.json".
```

Nuestro controlador devuelve un nuevo objeto **Response** de Symfony.

Hay varias clases diferentes de **Response**:

​	- **HttpFoundation:** componente (libreria) que contiene todo lo relacionado con peticiones y respuestas.

Cada vez que se hace referencia a un nombre de clase, se debe tener una declaración **use** correspondiente, de lo contrario PHP te dará un error diciendo que no puede encontrar la clase **Response**. 

```php
use Symfony\Component\HttpFoundation\Response;
```

Para crear una respuesta:

```php
return new Response(content, status, headers);
```



Ejemplo, el primer argumento es el contenido que queremos devolver, empieza con una cadena codificada.

```php
use Symfony\Component\HttpFoundation\Response;
class MainController
{
    #[Route('/')]
    public function homepage()
    {
        return new Response('<strong>Starshop</strong>: your monopoly-busting option for Starship parts!');
    }
}
```

Mostrará:

![image-20260330125051542](/home/soa/.config/Typora/typora-user-images/image-20260330125051542.png)

Es opcional, pero como el controlador siempre devuelve un Response, podemos añadir un tipo de retorno Response, eso no cambia el funcionamiento del código, pero lo hace más descriptivo de leer, y si alguna vez hiciéramos una tonteria y devolvíeramos algo que no fuera una respuesta, PHP nos lo recordaría claramente.

```php
public function homepage(): Response
```





NOTA: Dependiendo de lo que necesites devolver, Symfony ofrece formas más rápidas:

- **JSON (API):** En lugar de configurar los headers a mano, usa `JsonResponse`:

  PHP

  ```
  use Symfony\Component\HttpFoundation\JsonResponse;
  
  return new JsonResponse(['data' => '123']);
  // O desde un AbstractController:
  return $this->json(['data' => '123']);
  ```

- **Renderizar una plantilla (Twig):** Si heredas de `AbstractController`, el método `render` ya devuelve un objeto `Response`:

  PHP

  ```
  return $this->render('user/profile.html.twig', [
      'name' => 'Juan',
  ]);
  ```



## Recetas Flex Mágicas

Cuando se crea un proyecto con **symfony new**:

- se clona un repositorio llamado **symfony/skeleton** que es sólo un archivo si ignoras la licencia
- se ejecuta **composer install**

Los directorios **bin/, config/ y src/** se crean con un paquete especial dentro del **composer.json** llamado **symfony/flex**.

**Flex** es un complemento de Composer que añade dos superpoderes a Composer: alias y recetas.

parte de código de composer.json:

```json
{
    "require": {
        "symfony/flex": "^2",
    },
}
```



#### Alias Flex:

Los alias son sencillos, para añadir un nuevo paquete a tu aplicación:

```bash
composer require nombre_paquete
```

Ejemplo:

```bash
composer require symfony/http-client
```

Flex da a los paquete más importantes de Symfony un nombre más corto, llamado **alias.**

Por ejemplo, **symfony/http-client** tiene un alias **http-client** ,

Para ver todos los alias disponibles, hay que ir a un repositorio llamado **symfony/recetas**:

https://github.com/symfony/recipes/blob/flex/main/RECIPES.md



#### El sistema de recetas

Symfony Flex añade a Composer las recetas. Cuando se añade un nuevo paquete, puede tener una receta, que es un conjunto de archivos que se añadirán a tu proyecto. Y resulta que todos los archivos con los que empezamos en **bin/, config/, public**- proceden de las recetas de los paquetes que se instalaron originalmente.

Por ejemplo: **symfony/framework-bundle** es el paquete "core" (nucleo o centro) del Framework Symfony. Puedes comprobar su receta yendo al repositorio **symfony/recipes** y navegando a **symfony, framework-bundle** y luego a la última versión. Echa un vistazo a **config/packages**. (La mayoría de las cosas con las que empezamos proceden de esta receta).

Otra forma de ver las recetas, es en la línea de comandos:

```bash
composer recipes
```

Mostraría algo como:

![image-20260330172146096](/home/soa/.config/Typora/typora-user-images/image-20260330172146096.png)

Aparentemente se instalaron las recetas de cuatro paquetes diferentes, podríamos obtener información sobre cualquiera de ellos añadiendo su nombre al final del comando. Ejemplo:

```bash
composer recipes symfony/console
```

![image-20260330172737694](/home/soa/.config/Typora/typora-user-images/image-20260330172737694.png)

Con las recetas podemos instalar un paquete y obtener al instante cualquier archivo que necesitemos, en lugar de complicarnos con la configuración, nos ponemos manos a la obra.



#### Instalar PHP CS Fixer

Para probar añadimos el paquete PHP-CS-Fixer que proporcionará un archivo ejecutable para arreglar el estilo de nuestro código. Por ejemplo, en **src/Controller/MainController.php**, si sigues las normas de codificación de PHP, la llave debe estar en la línea siguiente a una función. Si hicieramos algo así, nuestro archivo violaría ahora esas normas. Eso no dañaría nada, pero queremos que tenga un aspecto limpio.

Para instalarlo:

```bash
composer require cs-fixer-shim
```

cs-fixer-shim es un alias del paquete `php-cs-fixer/shim` y tiene la receta `Configuring php-cs-fixer/shim`

También podemos verlo ejecutando: git status

![image-20260330174349844](/home/soa/.config/Typora/typora-user-images/image-20260330174349844.png)

El hecho de que `composer.json` y `composer.lock` estén modificados es un comportamiento 100% normal de Composer. Puedes ver que `composer.json` tiene la nueva biblioteca bajo la clave `require`.

```json
{
    "require": {
        "php-cs-fixer/shim": "^3.46",
    },
}
```

Pero todos los demás archivos modificados o nuevos lo son gracias a la receta del paquete.



#### Investigando la receta

Abre **.gitignore**, en la parte inferior ha añadido dos nuevas entradas para dos archivos comunes que querrás ignorar cuando se utilice PHP CS fixer.

```
###> php-cs-fixer/shim ###
/.php-cs-fixer.php
/.php-cs-fixer.cache
###< php-cs-fixer/shim ###
```

La receta también añadió un nuevo archivo `.php-cs-fixer.dist.php`. que es el archivo de configuración de CS Fixer.

```php
<?php

$finder = (new PhpCsFixer\Finder())
    ->in(__DIR__)
    ->exclude('var')
;

return (new PhpCsFixer\Config())
    ->setRules([
        '@Symfony' => true,
    ])
    ->setFinder($finder)
;
```

Esto arregla todos los archivos del directorio actual, pero ignora el directorio **var/** donde Symfony almacena sus archivos de caché. También le dice que utilice un conjunto de reglas llamado Symfony, lo que significa que el estilo de nuestro código coincida con el estilo de Symfony.

El último archivo modificado es **symfony.lock**, este mantiene un registro de que recetas tenemos instaladas y en que versión.



#### Utilizar PHP-CS-Fixer

Para utilizar este paquete, ejecutar:

```bash
./vendor/bin/php-cs-fixer
```

Eso mostrará todos los comandos disponibles, el que vamos a probar es fix, para probarlo:

```bash
./vendor/bin/php-cs-fixer fix
```

Este comando ha encontrado la infracción en **MainController.php**, cuando vamos a ese archivo, la llave rizada desde el final de la línea a la siguiente línea. 

![image-20260331192110188](/home/soa/.config/Typora/typora-user-images/image-20260331192110188.png)



Otra de las bibliotecas favoritas de todo PHP, es el motor de pantillas Twig.



## Twig

Se utiliza para devolver HTML a la página sin ponerlo dentro del controlador, para que no sea feo, utilizamos la biblioteca de plantillas Twig. 



**Instalación de Twig**

`Tiwg` es un alias del paquete `symfony/twig-pack`, este paquete instala tres nuevos paquetes

Un paquete es una especie de paquete falso (atajo) que ayuda a instalar varios paquetes a la vez.

```json
{
    "require": {
        "symfony/twig-bundle": "7.0.*",
        "twig/extra-bundle": "^2.12|^3.0",
        "twig/twig": "^2.12|^3.0"
    },
}
```

**Paquetes Symfony**

Ejecutamos `git status`para ver que ha hecho la receta.

Vemos los habituales **composer.json, composer.lock y simfony.lock**, pero por primera vez, también vemos una modificación de **config/bundles.php** .

Un **bundle** es un paquete PHP que se integra con Symfony, es básicamente un plugin de Symfony, siempre que se instala un bundle hay que activarlo en el archivo bundle.php. Pero el sistema de recetas lo hará por nosotros, así nunca editaremos este archivo a mano.

Archivo config/bundles.php

```php
<?php

return [
    Symfony\Bundle\FrameworkBundle\FrameworkBundle::class => ['all' => true],
    Symfony\Bundle\TwigBundle\TwigBundle::class => ['all' => true],
    Twig\Extra\TwigExtraBundle\TwigExtraBundle::class => ['all' => true],
];
```



### La receta Twig

Lo segundo que hizo la receta fue crear un archivo `config/packages/twig.yaml`. El propósito de cada archivo en `config\packages/` es configurar un bundle.

Archivo: config/packages/twig.yaml

```php
twig:
    default_path: '%kernel.project_dir%/templates'
when@test:
    twig:
        strict_variables: true

```

Por ejemplo, `twig.yaml` controla el comportamiento de TwigBundle. Esta línea de aquí le dice a Twig:

> ¡Oye! Todos mis archivos de plantilla terminarán en `.twig`.

Lo último que hizo la receta fue añadir un directorio **templates/**, que es donde se almacenarán los archivos de plantilla, inicialmente contiene base.html.yaml



### Renderizar una plantilla

Para renderizar una plantilla primero hacemos que el controlador extienda de una clase base llamada **AbstractController.**

Asegurate de pulsar el tabulador, para que se añáda la sentencia **use** en la parte superior. Extender esta clase base es opcional, pero nos proporciona un montón de métodos abreviados.

Archivo  src/Controller/MainController.php

```php
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
class MainController extends AbstractController
{
}
```

Por ejemplo, copia la cadena y luego, para renderizar una plantilla escribe `return $this->render()` y pasa un nombre de archivo a una plantilla. Utiliza: `main:homepage.html.twig`

```php
class MainController extends AbstractController
{
    #[Route('/')]
    public function homepage(): Response
    {
        return $this->render('main/homepage.html.twig');
    }
}
```

El nombre del archivo de tu plantilla puede ser el que quieras, pero lo estándar es tener un directorio que coincida con el nombre de tu controllador y un nombre de archivo que coincida con el nombre de ti método.

Creamos en `templates/`un nuevo directorio llamado `main` y dentro de él, un archivo llamado `homepage.html.twig`. Luego añade un **h1** y ponlo alrededor de todo.

```html
<h1>
    Starshop: your monopoly-busting option for Starship parts!
</h1>
```

El controlador sigue devolviendo un Response, render() es un atajo para renderizar la plantilla, coger esa cadena de HTML y ponerla en un objeto Response.



### Pasar datos a una plantilla

Como todavía no tenemos una base de datos a consultar, vamos a fingir un valor $starshipCount =457.

Para pasar variables a la plantilla, hay que añadir un segundo argumento a **render():** una matriz. Pasa **numberOfStarships** ajustado a $starshipCount. La clave se convertirá en el nombre de la variable dentro de la plantilla Twig.

```php
class MainController extends AbstractController
{
    #[Route('/')]
    public function homepage(): Response
    {
        $starshipCount = 457;

        return $this->render('main/homepage.html.twig', [
            'numberOfStarships' => $starshipCount,
        ]);
    }
}
```

### Renderizar variables

En la plantilla, añadiré un div y algo de texto. Para imprimir el número, escribe {{el nombre de la variable}}

```html
<div>
    Browse through {{ numberOfStarships }} starships!
</div>
```

Twig es su propio ñenguaje, solo tiene tres sintaxis diferentes: {{ variable }} (parecido a javascript).



### Etiquetas Twig y la sintaxis "hacer algo"

Por ejemplo, podríamos imprimir la cadena `'numberOfStarships'`... o la variable `numberOfStarships`... o incluso `numberOfStarships` veces 10.

Archivo templates/main/homepage.html.twig

```html
<div>
    Browse through {{ numberOfStarships * 10 }} starships!
</div>
```

La segunda sintaxis ("hacer algo") de las tres, empieza por {%, esto no imprime nada, en su lugar, se utiliza para construcciones del lenguaje como las sentencias if, los bucles for o establecer una variable.

Para hacer una sentencia if, di `if numberOfStarships >400` y cierrala con `{% endif %}`. Dentro añadiré un comentario.

Archivo: templates/main/homepage.html.twig

```html
<div>
    Browse through {{ numberOfStarships * 10 }} starships!

    {% if numberOfStarships > 400 %}
        <p>
            That's a shiploads of ships!
        </p>
    {% endif %}
</div>
```



**Twig** es una biblioteca mantenida por Symfony, por lo que sus documentos están en https://twig.symfony.com

Haz clic en el enlace "Docs" y desplazate hacia abajo para ver las etiquetas (Tags) con las que se puede "hacer algo". Por ejemplo, no puedes decir `{% appleasauce...`simplemente no funcionará. Sólo puedes usar `{%`y luego una de estas etiquetas, probablemente sólo utilice 5 de ellas a diario.

La tercera y última sintaxis de Twig, es para los comentarios `{#`para escribir un comentario. 

```html
<div>
    Browse through {{ numberOfStarships * 10 }} starships!

    {% if numberOfStarships > 400 %}
        <p>
            {# Do you think "shiploads" will pass the legal team? #}
            That's a shiploads of ships!
        </p>
    {% endif %}
</div>
```



#### Representación de una matriz asociativa

Twig además puede manejar cualquier dato complejo que le pases. Por ejemplo, en el controlador, crea una nueva variable **$myShip**, configurada como una matriz asociativa. Luego pásala a la plantilla como una nueva variable myShip.

archivo: src/Controller/MainController.php

```php
class MainController extends AbstractController
{
    #[Route('/')]
    public function homepage(): Response
    {
        $starshipCount = 457;
        $myShip = [
            'name' => 'USS LeafyCruiser (NCC-0001)',
            'class' => 'Garden',
            'captain' => 'Jean-Luc Pickles',
            'status' => 'under construction',
        ];

        return $this->render('main/homepage.html.twig', [
            'numberOfStarships' => $starshipCount,
            'myShip' => $myShip,
        ]);
    }
}
```

En la plantilla, añadimos otro <div>... algo de texto y una tabla para imprimir los datos. En el `<td>`, no podemos imprimir `myShip`...porque imprimir una matriz asociativa no tiene sentido en php.. y por tanto no tiene sentido en Twig. Obtendrás el famoso error sobre la conversión de array a cadena.

Lo que queremos es imprimir la clave `name` de esa matriz, se hace como en javascrip:`myShip.name.

```
<div>
    <h2>My Ship</h2>

    <table>
        <tr>
            <th>Name</th>
            <td>{{ myShip.name }}</td>
        </tr>
        <tr>
            <th>Class</th>
            <td>{{ myShip.class }}</td>
        </tr>
        <tr>
            <th>Captain</th>
            <td>{{ myShip.captain }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ myShip.status }}</td>
        </tr>
    </table>
</div>
```



#### Funciones y filtros Twig

Los filtros son funciones con una sintaxis más fresca y moderna. Por ejemplo, hay un filtro llamado `upper` para enviar una cadena a mayúsculas.

Para utilizar un filtro, busca la cadena que quieres convertir a mayúsculas y añade `|` y `upper`.  

```html
<td>{{ myShip.captain|upper }}</td>
```

El valor de la izquierda se pasa a través del filtro (muy parecido a utilizar una tubería en la línea de comandos).

*Una tubería (pipe) en la línea de comandos, representada por el símbolo barra vertical `|`, redirige la salida estándar de un comando como entrada estándar de otro. Permite encadenar comandos para procesar datos secuencialmente, filtrarlos o transformarlos, sin crear archivos temporales intermedios. Ejemplo: `ls -l | grep ".txt"`



### Herencia de plantillas Twig

La receta de Twig añadió un archivo de diseño base llamado `base.html.twig`.

```html
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
```

Aquí es donde añadiremos la navegación superior, el pie de página y cualquier otra cosa que deba aparecer en cada página.

**Ampliando el diseño base**

Con una función llamada herencia de plantillas. En `homepage.html.twig`, en la parte superior, escribe `{%extends}` y luego el nombre de la plantilla base: `base.html.twig`.

```html
{% extends 'base.html.twig' %}

<h1>
    Starshop: your monopoly-busting option for Starship parts!
</h1>
```

Si no hacemos nada más y actualizamos, obtendremos un error:

**"Una plantilla que extiende otra no puede incluir contenido fuera de los bloques Twig".**

Cuando extiendes una plantilla, le dices a Twig que quieres renderizar tu plantilla dentro de ese diseño base.

Para decirle donde queremos el contenido, hay que utilizar bloques, que son huecos en los que una plantilla hija puede poner contenido. Lo pondremos en el bloque `body`, para ponerlo ahí, rodea todo el contenido con un `{%block body%}`y en la parte inferior `{%endblock body%}`

```html
{% extends 'base.html.twig' %}

{% block body %}
<h1>
    Starshop: your monopoly-busting option for Starship parts!
</h1>

<div>
    Browse through {{ numberOfStarships * 10 }} starships!

    {% if numberOfStarships > 400 %}
        <p>
            {# Do you think "shiploads" will pass the legal team? #}
            That's a shiploads of ships!
        </p>
    {% endif %}
</div>

<div>
    <h2>My Ship</h2>

    <table>
        <tr>
            <th>Name</th>
            <td>{{ myShip.name }}</td>
        </tr>
        <tr>
            <th>Class</th>
            <td>{{ myShip.class }}</td>
        </tr>
        <tr>
            <th>Captain</th>
            <td>{{ myShip.captain|upper|lower|title }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ myShip.status }}</td>
        </tr>
    </table>
</div>
{% endblock %}
```

No parece muy diferente, pero esta vivo, estamos dentro del diseño base. Esto se llama herencia de plantillas por que funciona igual que la herencia de clases PHP. Imagina que tienes una clase **Homepage** que extiende a una clase **Base**. En esa clase Base tiene un método **body()**, y nosotros anulamos ese método **body()** en la clase **Homepage**. Es el mismo concepto en Twig.



#### Reemplazar el titulo de la página.

Y estos nombres de bloques -como **javascripts, stylsheets y body** no son nombres especiales y no están registrados en ninguna parte. Puede crear nuevos bloques como quiera y cuando quieras. Por ejemplo, supongamos que queremos cambiar el **title** de la página desde una plantilla hija. En este caso, la receta ya nos ha proporcionado un bloque llamado **title** para hacerlo, y este bloque tiene contenido por defecto... por eso ya vemos **Welcome** en la pestaña del navegador. 

Anulemos esto en nuestra plantilla.

archivo: templates/base.html.twig

```html
<html>
    <head>
        <title>{% block title %}Welcome!{% endblock %}</title>
    </head>
</html>
```

En cualquier lugar fuera del bloque **body**, di `{% block title %}`, escribe algo y luego `{% enblock %}`

archivo: template/main/homepage.html.twig

```html
{% extends 'base.html.twig' %}

{% block title %}Starshop: Beam up some parts!{% endblock %}

{% block body %}
{% endblock %}
```



#### Sustituir frente a añadir el bloque padre

Con lo que hemos hecho hemos sustituido la palabra Welcome, por el titulo nuevo, pero ocasionalmente, en lugar de sustituir el bloque padre se quiera añadir. Se puede hacer con

```html
{{parent()}}
```

Esta función coge el contenido del bloque `title` de la plantilla padre. Luego utilizamos `{{ para imprimirlo. Esta vez vemos la bienvenida y luego nuestro título.

archivo: /templates/main/homepage.html.twig

```html
{% extends 'base.html.twig' %}

{% block title %} {{parent()}}Starshop: Beam up some parts!{% endblock %}

{% block body %}
{% endblock %}
```



### Depurando con el Asombroso Perfilador

Para instalar las herramientas de depuración:

```bash
composer require debug
```

Esto instala 4 paquetes en composer.json :

1. monolog-bundle: monolog es una biblioteca de registro. 

   ```json
   {
       "require": {
           "symfony/monolog-bundle": "^3.0",
       },
   }
   ```

2. tres dependencias de desarrollo que impulsan el "perfilador"

   ```
   {
       "require-dev": {
           "symfony/debug-bundle": "7.0.*",
           "symfony/stopwatch": "7.0.*",
           "symfony/web-profiler-bundle": "7.0.*"
       }
   }
   ```

Para ver los paquetes que se han instalado, escribir en la terminal git status.

![image-20260403123302647](/home/soa/.config/Typora/typora-user-images/image-20260403123302647.png)

habilitó unos cuantos bundles nuevos y tres archivos de configuración nuevos para esos bundles. Los registros aparecerán en un directorio `/var /log`.

**Hola barra de herramientas de depuración web y perfilador** 

Se añade una negra en la parte inferior llamada *barra de herramientas de depuración web*.

![image-20260403124041969](/home/soa/.config/Typora/typora-user-images/image-20260403124041969.png)

Donde podemos ver la ruta y el controlador de esta página, así encontraremos rápidamente el código que hay detrás, cuanto tardó en cargarse la página, cuanta memoria utilizó, e incluso la plantilla Twig que se renderizó y cuánto tardó.

Cuando se hace clic en cualquiera de estos enlaces: saltas al perfilador, éste tiene diez veces más de información: detalles sobre la petición y la respuesta, registros que se produjeron mientras se cargaba la página, detalles de enrutamiento e incluso estadísticas sobre lass plantillas Twig que se procesaron. 

Aparentemente, se estaban renderizando seis plantillas:

- la principal
- el diseño base
- algunas otras que alimentan la barra de herramientas de depuración web. (no se mostrarán cuando pasemos a producción).

Rendimiento: aquí se divide todo el tiempo de carga de nuestra página en diferentes partes, así se puede saber que parte del código puede estar ralentizando la página...



#### Hola bin/console

Ejecuta en la línea de comandos:

```bash
php bin/console o ./bin/console
```

Esta es la consola de Symfony, contiene muchos comandos e incluso se pueden añadir tus propios comandos, muchos de estos comandos empiezan por debug (comandos muy útiles), como :

**debug:router**:

```bash
php bin/console debug:router
```

Esto nos muestra todas las rutas de nuestra aplicación: la ruta de la página de inicio en la parte inferior y un montón de rutas añadidas por Symfony en el entorno `dev` que alimentan la barra de herramientas de depuración web y el perfilador:

![image-20260403131146131](/home/soa/.config/Typora/typora-user-images/image-20260403131146131.png)

**debug:twig:**

```bash
php bin/console debug:twig
```

Indica todas las funciones, filtros y otros elementos de Twig que existen en nuestra aplicación, es como la documentación de Twig, salvo que también incluye funciones y filtros adicionales añadidos a Twig por los bundles que hemos instalado.



## Creación de rutas API JSON

API JSON: Es una interfaz de programación de aplicaciones que utiliza el formato ligero JSON (JavaScript Object Notation) para intercambiar datos entre un servidor y una aplicación web o móvil.

Aspectos claves de una API JSON:

- **Formato de Datos:** Utiliza pares clave-valor (ej. `"nombre": "Juan"`) para estructurar la información, lo que facilita su lectura.
- **Independencia de Lenguaje:** Aunque se basa en la sintaxis de JavaScript, es compatible con casi todos los lenguajes de programación modernos (Python, PHP, Java, etc.).
- **Uso Común (REST):** La mayoría de las APIs modernas (especialmente REST) devuelven respuestas en formato JSON porque es más ligero y rápido que XML.
- **Eficiencia:** Es ideal para aplicaciones web modernas que requieren actualizaciones rápidas de datos sin recargar la página



Se puede crear una API con Symfony con API Platform (marco para crear APIs robustas)

### Creación de la nueva Ruta y Controlador

Ésta será nuestra segunda página (segunda combinación de ruta y controlador). 

Creamos un archivo StarshipApiController.php extendiendo la clase `AbstractController.

```php
<?php

namespace App\Controller;

use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;

class StarshipApiController extends AbstractController
{
    #[Route('/api/starships')]
    public function getCollection(): Response
    {
        $starships = [
            [
                'name' => 'USS LeafyCruiser (NCC-0001)',
                'class' => 'Garden',
                'captain' => 'Jean-Luc Pickles',
                'status' => 'taken over by Q',
            ],
            [
                'name' => 'USS Espresso (NCC-1234-C)',
                'class' => 'Latte',
                'captain' => 'James T. Quick!',
                'status' => 'repaired',
            ],
            [
                'name' => 'USS Wanderlust (NCC-2024-W)',
                'class' => 'Delta Tourist',
                'captain' => 'Kathryn Journeyway',
                'status' => 'under construction',
            ],
        ];

        return $this->json($starships);
    }
}
```

El método getCollection devolverá información sobre una colección de naves estelares. La URL de la ruta vamos a poner `/api/starships`. En el interior del método creamos una matriz de tres matrices asociativas de datos de naves estelares.



### Devolver el JSON

Para convertir la respuesta en formato JSON:

```php
return new Response(json_encode($starships));
```

o directamente con:

```php
$this->json($starships);
```

Para probarlo poner en el navegador:

```
http://localhost:8000/api/starships
```

![image-20260403163746909](/home/soa/.config/Typora/typora-user-images/image-20260403163746909.png)

Para que el JSON aparezca estilizado, marcar en el check que poner dar formato al texto o instalar en Chome la extensión JSONVue, pulsando en configuración-> extensiones e instalar

![image-20260403164024536](/home/soa/.config/Typora/typora-user-images/image-20260403164024536.png)

 Después de instalar dicha extensión se verá así:

![image-20260403164110438](/home/soa/.config/Typora/typora-user-images/image-20260403164110438.png)



### Añadir una clase modelo

En el mundo real, cuando empecemos a consultar la base de datos, vamos a trabajar con objetos, no con matrices asociativas, por ahora como no vamos a añadir una base de datos, para hacer las cosas más realistas, en lugar de utilizar matrices, vamos a utilizar objetos para los datos. En el directorio **src/** vamos a crear un subdirectorio **Model.**

Dentro creamos una nueva clase `Starship` (archivo Starship.php) 

```php
<?php

namespace App\Model;

class Starship
{
    public function __construct(
        private int $id,
        private string $name,
        private string $class,
        private string $captain,
        private string $status,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getClass(): string
    {
        return $this->class;
    }

    public function getCaptain(): string
    {
        return $this->captain;
    }

    public function getStatus(): string
    {
        return $this->status;
    }
}
```

Creamos un constructor con cinco propiedades, para simular la matriz asociativa que teníamos antes (id, name, class, captain y status ), 

```php
public function __contruct()
```

Hemos creado las propiedades **privadas**, por lo que si instanciaramos un nuevo objeto `Starship` no podríamos leer ninguno de los datos. Para permitirlo, hay que crearlos métodos getter, esto se puede hacer:

- A mano (no es lo recomendable)

- Usando el MakerBundle de Symfony (La más recomendable), ya que es la forma más limpia, porque Symfony lee tus propiedades y genera el código exacto que espera el framework, desde la terminal:

  ```bash
  php bin/console make:entity --regenerate
  ```

  Lo que hace es busca todas tus entidades, y si ve que tienen propiedades pero no los métodos, los crea automáticamente por ti.

  También se puede usar `php bin/console make:entity` y escribir el nombre de una clase que ya exista; el asistente te preguntará si quieres añadir más campos o simplemente generar los métodos faltantes.

- Con la extensión de visual studio code php getter & setter, en la que pulsando en el botón derecho en el desplegable ahora aparece las opciones:

  ![image-20260403170812348](/home/soa/.config/Typora/typora-user-images/image-20260403170812348.png)

  Para que cree las líneas de código hay que situarse encima de cada propiedad y pulsar en botón derecho y seleccionar la opción que queremos utilizar.

- El truco de "Intellisense" (sin instalar nada nuevo , si tenemos instalado PHP Intelephense (que es casi obligatorio para programar en PHP con VS Code))

  - Escribe la propiedad: private $id;
  - En una línea vacía dentro de la clase, empieza a escribir get... o set...
  - El autocompletado te sugerirá getId basándose en tus variables, al darle al Enter, te generará la estructura básica.

- #### PHP 8: El "Modo Dios" (Promotion Properties)

  Si usas PHP 8.0 o superior, puedes ahorrarte incluso el declarar las propiedades y los Getters si solo los quieres para inicializar. Se llama **Constructor Property Promotion**:

  ```php
  // Esto declara la propiedad, el parámetro y lo asigna en una sola línea
  public function __construct(
      private string $username,
      private string $password,
  ) {}
  ```

  Resumen de atajos

  - **Symfony:** `make:entity --regenerate` (Terminal).
  - **VS Code:** Clic derecho -> **Source Action...** (si tienes instalada una extensión de PHP Pack).



### Crear los objetos modelo

Para convertir estas matrices que usábamos en objetos: **new Starship()**- pulsa tabulador, para que añada la declaración **use**, luego dale un **id**, que tal, 1 y transfiere los otros valores para **name, class, captain y status**. 

Archivo: src/Controller/StarshipApiController.php

```php
<?php

namespace App\Controller;

use App\Model\Starship;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;

class StarshipApiController extends AbstractController
{
    #[Route('/api/starships')]
    public function getCollection(): Response
    {
        $starships = [
            new Starship(
                1,
                'USS LeafyCruiser (NCC-0001)',
                'Garden',
                'Jean-Luc Pickles',
                'taken over by Q'
            ),
            new Starship(
                2,
                'USS Espresso (NCC-1234-C)',
                'Latte',
                'James T. Quick!',
                'repaired',
            ),
            new Starship(
                3,
                'USS Wanderlust (NCC-2024-W)',
                'Delta Tourist',
                'Kathryn Journeyway',
                'under construction',
            ),
        ];

        return $this->json($starships);
    }
}
```

Ahora tenemos una matriz de 3 objetos `Starship`, que los pasamos a `$this->json()`, pero esto ya no funciona, estos objetos están vacios, eso es por que, internamente,`$this->json` utiliza la función json_encode() y esa función no puede manejar propiedades privadas. 

Lo que necesitamos es algo que pueda reconocer la propiedad que, aunque la propiedad `name` es privada tenemos un método público `getName()` al que se puede llamar para leer el valor de esa propiedad.



Se puede instalar también la extensión de visual Studio Code : php-cs-fixer



### Hola Serializador Symfony

El serializador de Symfony toma objetos y los serializa a JSON... o toma JSON y deserializarlo de nuevo a objetos. 

Puede manejar totalmente situaciones en las que tienes propiedades privadas con métodos getter públicos.

Para instalarlo:

```bash
composer require serializer
```

Ahora después de instalarlo ya funciona, y eso es porque el método `$this->json()` es inteligente, para verlo, mantén pulsado Ctrl y haz clic sobre el método para saltar al archivo principal de Symfony donde se encuentra, y vemos que detecta el sistema serializador que transforma el objeto a JSON

archivo interno de Symfony donde esta el método json:

```php
/**
     * Returns a JsonResponse that uses the serializer component if enabled, or json_encode.
     *
     * @param int $status The HTTP status code (200 "OK" by default)
     */
    protected function json(mixed $data, int $status = 200, array $headers = [], array $context = []): JsonResponse
    {
        if ($this->container->has('serializer')) {
            $json = $this->container->get('serializer')->serialize($data, 'json', array_merge([
                'json_encode_options' => JsonResponse::DEFAULT_ENCODING_OPTIONS,
            ], $context));

            return new JsonResponse($json, $status, $headers, true);
        }

        if (null === $data) {
            return new JsonResponse('null', $status, $headers, true);
        }

        return new JsonResponse($data, $status, $headers);
    }
```

El serializer es como un "traductor", es decir convierte objetos de PHP a otros formatos (JSON, XML, CSV) y viceversa. Es especialmente útil cuando se construye una API.

El serializador hace:

1. Normalización: transforma un objeto complejo en un Array de PHP
2. Codificación (encoding): transforma ese array en una cadena de texto en el formato final. Ej: JSON.

Para el proceso inverso (deserialización): es lo mismo pero a la inversa

1. Decodign: de JSON a Array
2. Denormalización : de array a objeto.



Se usa en lugar de json_encode($objeto) porque json_encode falla con objetos complejos o relaciones circulares.

**La gran diferencia con el Serializer de Symfony**

Ya que estuvimos hablando de Symfony, es importante notar la diferencia:

- **`json_encode`**: Es una función simple de bajo nivel. Solo entiende de arrays y tipos básicos. Si le pasas un objeto complejo (como una Entidad de base de datos con relaciones), lo más probable es que devuelva un JSON vacío `{}` porque no puede acceder a las propiedades privadas.
- **Serializer de Symfony**: Es una herramienta de alto nivel. Utiliza "Reflexión" para leer propiedades privadas, maneja relaciones entre tablas, permite usar grupos de seguridad y formatea fechas automáticamente antes de llamar internamente a `json_encode`.

> **En resumen:** Usa `json_encode` para cosas rápidas y simples con arrays. Usa el **Serializer** cuando trabajes con objetos complejos y lógica de negocio en Symfony.



## Los servicios: la columna vertebral de todo

Es el concepto más importante en Symfony, un servicio es un objeto que hace un trabajo, por ejemplo, si instancias un objeto `Logger` que tiene un método `log()`, eso es un servicio, funciona: registra cosas.

O si creaste un objeto de conexión a la base de datos que hace consultas a la base de datos, entonces eso también es un servicio.

Entonces, si un servicio es sólo un objeto que funciona, nuestra clase Starship es un ejemplo de no-servicio, su función principal no es hacer trabajo: es guardar datos. Claro, tiene unos cuantos métodos públicos... e incluso podrías poner algo de lógica dentro de estos métodos para hacer algo, pero en última instancia, no es un trabajador, es un poseedor de datos.

¿Y las clase controladoras? Sí, también son servicios. Su trabajo consiste en crear objetos de respuesta.

De todas formas, todo el trabajo que hace Symfony lo hace en realidad un servicio. ¿Escribir mensajes de registro en este archivo? Sí, hay un servicio para eso. ¿Descubrir qué ruta coincide con la URL actual?, ese es el servicio **router**. ¿Y la representación de una plantilla Twig? Sí, resulta que el método **render()** es un ayajo para encontrar el objeto de servicio correcto y llamar a un método en el.

Por tanto, un **servicio** es un objeto de PHP que realiza una tarea específica y reutilizable para toda tu aplicación.

Si una **Entidad** es el "qué" (los datos, como un Usuario), un **Servicio** es el "cómo" (la lógica, como enviar un email, calcular impuestos o conectar con una API externa).

### El contenedor y debug:container

Estos servicios están organizados en un gran objeto llamado "contenedor de servicios", es como una gigantesca matriz asociativa de objetos de servicio, cada uno con un identificador único.

Para ver la lista de todos los servicios desde la terminal ejecuta:

```bash
bin/console debug:container
```

 Mostrará algo como :

![image-20260404103011189](/home/soa/.config/Typora/typora-user-images/image-20260404103011189.png)

Aquí es donde Symfony se vuelve potente. Symfony tiene un "almacén" gigante llamado **Service Container**.

Cuando arranca la aplicación, Symfony lee todas tus clases en `src/` y las registra como servicios automáticamente (gracias a una función llamada *autowiring*). Tú no tienes que instanciarlos con `new`; Symfony te los entrega ya listos.

**Servicios Internos de Symfony**

Symfony ya viene con cientos de servicios "de fábrica" que tú usas todo el tiempo sin saberlo:

- **`LoggerInterface`**: Para escribir logs.
- **`EntityManagerInterface`**: Para hablar con la base de datos (Doctrine).
- **`MailerInterface`**: Para enviar correos.
- **`HttpClientInterface`**: Para hacer peticiones a otras webs.



### Los bundles proporcionan servicios

Los bundles nos proporcionan servicios y los servicios son herramientas. En pocas palabras un bundle es un paquete de archivos que añade una funcionalidad específica a tu aplicación.



### Autocableado (Autowiring)

Ejecuta un comando relacionado, para listar los servicios autocablebles.

```
php bin/console debug:autowiring
```

![image-20260404124236144](/home/soa/.config/Typora/typora-user-images/image-20260404124236144.png)

Es una de las funciones más potentes del framework porque permite que Symfony "adivine" y pase automáticamente los servicios que necesitas a tus clases sin que tengas que configurar nada manualmente en archivos YAML o XML.



### Autoconexión del servicio Logger

Para registrar algo desde nuestro controlador, necesitaremos un servicio que haga ese trabajo, para buscarlo, ejecutamos el comando anterior, pero buscando log.

```bash
php bin/console debug:autowiring log
```

![image-20260404124632748](/home/soa/.config/Typora/typora-user-images/image-20260404124632748.png)

Ha encontrado unos 10 servicios, todos ellos empiezan por `Psr\Log\LoggerInterface`, lo que nos dice que hay un servicio en el contenedor para un registrador, y para autoconectarlo utilizamos esta interfaz. 

Lo que significa que en el método del controlador donde queremos el logger, añadimos un argumento de tipo `LoggerInterface` -pulsa tabulador- y luego di `$logger`.

```php
use Psr\Log\LoggerInterface;
class StarshipApiController extends AbstractController
{
    public function getCollection(LoggerInterface $logger): Response
    {
    }
}
```

`LoggerInterface `se debe corresponder a la declaración `use` con `Psr\Log\LoggerInterface` de `debug:autowiring`.

Así Symfony verá esta sugerencia de tipo y dirá: "¡Oh! Como ese type-hint coincide con el tipo de autocableado de este servicio, deben querer que les pase ese objeto de servicio".

Añade dd($logger):dd() significa "volcar y morir" :

archivo: src/Controller/StarshipApiController.php

```php
use Psr\Log\LoggerInterface;
class StarshipApiController extends AbstractController
{
    public function getCollection(LoggerInterface $logger): Response
    {
        dd($logger);
    }
}
```

Si lo ejecutamos, si imprimió el objeto y luego detuvo la ejecución. Por lo tanto funciona, Symfony nos pasa un objeto `Monolog\Logger` que implementa ese `LoggerInterface`.

El truco que acabamos de hacer -llamado autocableado- funciona exactamente en dos lugares:

- los métodos de nuestro controlador 
- y el método `__construct()` de cualquier servicio.



### Controlar el comportamiento de los servicios

El servicio de `Logger`salió de un bundle, en este caso `MonologBundle`, para que podamos configurar por ejemplo ese servicio, para que se registre en un archivo diferente, se haría con `config\packages\monolog.yaml`

Esta configuración -incluida esta línea- configura `MonologBundle`lo que en realidad significa que configura cómo funcionan los servicios que nos proporciona MonologBundle, le dice al servicio `Logger`que registr en este archivo `dev.log`.



### Utilizar el Logger

Ahora que tenemos el servicio `Logger` para utilizarlo podemos leer la documentación, pero el editor nos ayudará. `LoggerInterface` tiene un montón de métodos. Utilicemos `->info`

archivo: src/Controller/StarshipApiController.php

```php
use Psr\Log\LoggerInterface;
class StarshipApiController extends AbstractController
{
    public function getCollection(LoggerInterface $logger): Response
    {
        $logger->info('Starship collection retrieved');
    }
}
```

Para ver si registró algo, podríamos comprobar el archivo `dev.log`o utlizar la sección Registro del perfilador para este petición. 

El archivo `dev.log`se encuentra en /var/log y mostrará:

![image-20260405092759094](/home/soa/.config/Typora/typora-user-images/image-20260405092759094.png)









# IMPORTANTE

Cada vez que se hace referencia a un nombre de clase, se debe tener una declaración **use** correspondiente, de lo contrario PHP te dará un error diciendo que no puede encontrar la clase **Response**. 

**Controlador:** método en una clase que construye la página.

**Página:** combinación de un controlador y una ruta, que define la URL de la página.

es decir, cada página es una ruta y un controlador... y que cada controlador devuelve una respuesta.

**Paquete**: es una especia de paquete falso (atajo) que ayuda a instalar varios paquetes a la vez.

**Bundle**: plugin de symfony habría que activarlo en el archivo config/bundle.php pero el sistema de recetas lo activa automaticamente al instalarlo.

**Perfilador**: (Symfony profiler) es una de las herramientas más potentes, es una barra de herramientas y un panel de control detallado que recopila información técnica sobre cada petición (request) que realizas a tu aplicación durante el desarrollo. 

**servicio** es un objeto de PHP que realiza una tarea específica y reutilizable para toda tu aplicación.

**serializador:** de Symfony toma objetos y los serializa a JSON... o toma JSON y deserializarlo de nuevo a objetos. 

**Entidad:** es una clase de PHP que representa una tabla en tu base de datos. Si tu base de datos tiene por ejemplo una tabla llamada Productos, en tu código tendrás una Entidad llamada Producto. 

Symfony utiliza un motor llamado **Doctrine** (un ORM o Object-Relational Mapper) para que no tengas que escribir SQL a mano. En lugar de eso manipulas objetos PHP y Doctrine se encarga de "traducirlos" a filas en la base de datos. 

Una entidad vive normalmente en la carpeta `src/Entity`. Se ve como una clase normal, pero tiene **Atributos** (metadatos) que le dicen a la base de datos cómo comportarse.

```php
#[ORM\Entity]
class Producto {
    #[ORM\Id]
    #[ORM\GeneratedValue]
    #[ORM\Column]
    private ?int $id = null;

    #[ORM\Column(length: 255)]
    private ?string $nombre = null;

    // Aquí irían los Getters y Setters que comentamos antes...
}
```

**Conceptos clave que debes conocer**

- **Atributos (`#[ORM\...]`):** Son esas líneas que empiezan con `#`. Sirven para configurar la columna: si puede ser nula, el tamaño del texto, si es la clave primaria, etc.
- **Repository:** Por cada entidad, suele haber una clase "Repository" (`src/Repository`). Es el lugar donde haces las consultas (ej: `buscarTodosLosProductosBaratos()`).
- **Persistencia:** Cuando creas un objeto de una entidad (`$p = new Producto()`), este solo vive en la memoria RAM. Para que se guarde en el disco duro (base de datos), necesitas el **EntityManager**.



**¿Cómo se crean? (El flujo de trabajo)**

En Symfony no creas las tablas en MySQL directamente. Sigues este orden:

1. **Creas la Entidad:** Usas el comando `php bin/console make:entity`. El asistente te pedirá el nombre de los campos.
2. **Creas la Migración:** Ejecutas `php bin/console make:migration`. Symfony compara tu código con tu base de datos actual y genera un archivo SQL con los cambios necesarios.
3. **Ejecutas la Migración:** Ejecutas `php bin/console doctrine:migrations:migrate`. Ahora la tabla ya existe en tu base de datos.



**Ejemplo de uso en un Controlador**

Una vez que tienes tu entidad, así es como la usas para guardar datos:

```php
public function crear(EntityManagerInterface $entityManager): Response {
    $producto = new Producto();
    $producto->setNombre('Teclado Mecánico');

    // Le decimos a Doctrine que queremos guardar este objeto
    $entityManager->persist($producto);

    // Ejecuta las consultas SQL (INSERT)
    $entityManager->flush();

    return new Response('Producto guardado con ID ' . $producto->getId());
}
```

**En resumen**

- **Entidad:** Es el "qué" (el objeto/datos).
- **Repository:** Es el "dónde buscar" (las consultas).
- **EntityManager:** Es el "gestor" (el que guarda, borra y actualiza).







**Comandos Composer**

composer recipes: lista los paquetes instalados

composer recipes nombre_paquete: da información de un paquete concreto.

composer require nombre_paquete: instala un paquete



Webs importantes;

https://symfonycasts.com/es/screencast/symfony/route-controller#play

https://symfony.com/doc/6.4/the-fast-track/en/index.html



Otros capítulos dentro del curso que me enviaron:

https://symfonycasts.com/courses

https://symfonycasts.com/es/screencast/netgen-layouts/twig-blocks



https://latteandcode.medium.com/20-trucos-para-symfony-4-493f309c0864







