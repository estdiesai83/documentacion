import { AlertaTipo2Module } from './alerta-tipo2.module';

describe('AlertaTipo2Module', () => {
  let alertaTipo2Module: AlertaTipo2Module;

  beforeEach(() => {
    alertaTipo2Module = new AlertaTipo2Module();
  });

  it('should create an instance', () => {
    expect(alertaTipo2Module).toBeTruthy();
  });
});
